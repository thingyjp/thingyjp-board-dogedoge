sudo: false

os:
  - linux

language:
  - c

services:
  - docker

before_install:
  - docker pull debian:testing

script:
  - docker build -t withgit .
  - docker run withgit /bin/sh -c "FORCE_UNSAFE_CONFIGURE=1 cd /root && make -C software/"
